import React from 'react';
import Trips from './components/Trips';

export const metadata = {
  title: 'About Page',
};

const Page = () => {
  return (
    <div>
      <Trips />
    </div>
  );
};

export default Page;
